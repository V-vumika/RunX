# Authors

RunX is built and maintained by:

- **Vumi** ([@V-vumika](https://github.com/V-vumika)) — project owner. Focus:
  self-contained visual components and UI panels (visualizers, landing page
  sections, interview-mode UI).
- **Shiv** ([@Shiv-Reddy](https://github.com/Shiv-Reddy)) — collaborator.
  Focus: the execution engine, the `Snapshot`/`ValueNode` data contract, the
  Zustand store, and anything touching many files at once.

See `CLAUDE.md`'s "How we work" section for the fuller task-split model, and
`.github/CODEOWNERS` for which paths each of us typically reviews.
